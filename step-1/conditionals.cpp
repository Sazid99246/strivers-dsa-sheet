#include <bits/stdc++.h>
using namespace std;

int main()
{
    // write a program that takes an input as an integer
    // and prints if you are adult or not
    int age;
    cin >> age;
    if (age >= 18)
    {
        cout << "Yes, you are an adult!" << endl;
    }
    else {
        cout << "You are not an adult!" << endl;
    }
    return 0;
}
